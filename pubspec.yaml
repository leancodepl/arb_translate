name: arb_translate
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.5

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  collection: ^1.18.0
  file: ^6.1.4
  google_generative_ai:
    git:
      url: "https://github.com/google/generative-ai-dart.git"
      ref: main
      path: pkgs/google_generative_ai
  intl: ^0.18.1
  meta: ^1.10.0
  yaml: ^3.1.2

dependency_overrides:
  http: ^1.1.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
